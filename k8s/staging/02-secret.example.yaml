# Template only — mirrors .env.example. Copy to 02-secret.yaml (gitignored), fill in
# real values, and apply that file instead. Never commit real secret values.
apiVersion: v1
kind: Secret
metadata:
  name: mizan-secrets
  namespace: mizan-staging
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
  JWT_SECRET: "changeme-generate-a-256-bit-secret"
  INTERNAL_SERVICE_TOKEN: "changeme-generate-a-random-secret"
  ANTHROPIC_API_KEY: "your-api-key-here"
  R2_ACCOUNT_ID: "your-account-id"
  R2_ACCESS_KEY_ID: "your-access-key"
  R2_SECRET_ACCESS_KEY: "your-secret-key"
  R2_ENDPOINT_URL: "https://your-account-id.r2.cloudflarestorage.com"
  SMTP_USERNAME: "your-smtp-username"
  SMTP_PASSWORD: "your-smtp-password"
